<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <solid
        android:color="@android:color/transparent" />

    <stroke
        android:width="1dp"
        android:color="@android:color/white" />

    <size
        android:width="@dimen/passcode_button_size"
        android:height="@dimen/passcode_button_size"/>

</shape>